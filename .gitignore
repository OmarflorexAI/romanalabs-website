node_modules/

# Screenshot artifacts (regenerated by `npm run screenshot`)
screenshot-desktop.png
screenshot-mobile.png
screenshot.png

# Stray Windows redirection artifact
nul

# Env files — never commit (no current .env, but defensive)
.env
.env.*
!.env.example

# Editor / agent state
.claude/

# Binaries that should never be in a static-site repo
tailwind.exe.exe
*.exe

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
